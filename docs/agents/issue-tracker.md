# Issue tracker

Engineering issues and PRDs live in GitHub Issues for `kwmlamar/caye`. Use the `gh` CLI from this repository so it resolves the configured remote automatically.

Use standard GitHub issue operations for creating, reading, labeling, commenting on, and closing issues. When a skill says to publish to the issue tracker, create a GitHub issue.
