# Domain docs

This is a single-context repository. Before working in a domain, read root `CONTEXT.md` and relevant records under `docs/adr/` when they exist. Their absence is not an error; proceed using the codebase's established vocabulary.

If a proposed change conflicts with an architectural decision record, surface the conflict explicitly.
