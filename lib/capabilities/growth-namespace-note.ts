import 'server-only'

// Compile-time marker for Growth Intelligence v1. The canonical namespace
// is added to CapabilityNamespace alongside this branch's catalog registration.
export const GROWTH_CAPABILITY_NAMESPACE = 'growth' as const
