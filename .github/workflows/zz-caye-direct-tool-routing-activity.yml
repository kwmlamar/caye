name: One-shot Caye Direct routing and activity patch

on:
  push:
    branches:
      - fix/caye-direct-tool-routing-activity

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply patch
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace(path, old, new):
              p = Path(path)
              s = p.read_text()
              if old not in s:
                  raise SystemExit(f'missing patch anchor in {path}: {old[:100]!r}')
              p.write_text(s.replace(old, new, 1))

          Path('lib/model-router/tool-bridge/founder-tool-selector.ts').write_text(r'''import 'server-only'
import { TOOL_REGISTRY } from '@/lib/caye-agent/tools/registry'

/**
 * OpenAI-compatible backends accept at most 128 tools per request. Caye has
 * grown beyond that, so Founder Direct cannot keep shipping the entire
 * back-office registry on every turn.
 *
 * This selector is deterministic and authority-neutral: it only decides which
 * real registry entries the model may SEE on this turn. It never creates a
 * synthetic tool, changes roles/risk, or bypasses any server-side gate.
 */
export const MAX_FOUNDER_DIRECT_TOOLS = 112

const STOP = new Set([
  'the','a','an','and','or','to','of','for','in','on','at','is','are','be','my','me','i','we','you','it','this','that','with','from','do','can','could','would','should','please',
])

function words(value: string): string[] {
  return value
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, ' ')
    .split(/\s+/)
    .filter((w) => w.length > 1 && !STOP.has(w))
}

const DOMAIN_HINTS: Array<{ re: RegExp; terms: string[] }> = [
  { re: /\b(job|jobs|career|apply|application|resume|recruit|hiring|interview|ats|greenhouse|lever)\b/i, terms: ['job','application','career','resume','candidate','ats','greenhouse','recruit','hiring'] },
  { re: /\b(research|intelligence|evidence|claim|belief|trend|market|source)\b/i, terms: ['research','intelligence','evidence','claim','belief','source','trend'] },
  { re: /\b(email|gmail|inbox|message|reply|draft)\b/i, terms: ['email','gmail','inbox','message','reply','draft'] },
  { re: /\b(booking|tour|reservation|guest|customer|lead|quote|payment)\b/i, terms: ['booking','tour','reservation','guest','customer','lead','quote','payment'] },
  { re: /\b(file|image|photo|pdf|document|attachment|artifact)\b/i, terms: ['file','image','photo','pdf','document','attachment','artifact'] },
  { re: /\b(goal|objective|direction|priority|plan|strategy)\b/i, terms: ['goal','objective','direction','priority','plan','strategy'] },
]

function scoreTool(message: string, tool: { name: string; description: string; risk: string }): number {
  const query = new Set(words(message))
  const hay = new Set(words(`${tool.name} ${tool.description}`))
  let score = 0
  for (const token of query) if (hay.has(token)) score += 12

  const loweredName = tool.name.toLowerCase()
  for (const hint of DOMAIN_HINTS) {
    if (!hint.re.test(message)) continue
    if (hint.terms.some((term) => loweredName.includes(term) || hay.has(term))) score += 45
  }

  // Read tools are useful grounding primitives and cheap to expose when ties
  // remain. This is only a manifest-ranking tie-breaker, not execution policy.
  if (tool.risk === 'read') score += 1
  return score
}

export function selectFounderToolNames(message: string, maxTools = MAX_FOUNDER_DIRECT_TOOLS): string[] {
  const eligible = TOOL_REGISTRY.filter((tool) => tool.modes.includes('back-office'))
  if (eligible.length <= maxTools) return eligible.map((tool) => tool.name)

  return eligible
    .map((tool, index) => ({ tool, index, score: scoreTool(message, tool) }))
    .sort((a, b) => b.score - a.score || a.index - b.index)
    .slice(0, Math.max(1, Math.min(maxTools, 127)))
    .map(({ tool }) => tool.name)
}
''')

          Path('lib/model-router/tool-bridge/founder-tool-selector.test.ts').write_text(r'''import { describe, expect, it } from 'vitest'
import { MAX_FOUNDER_DIRECT_TOOLS, selectFounderToolNames } from './founder-tool-selector'

vi.mock('server-only', () => ({}))

import { vi } from 'vitest'

describe('Founder Direct tool manifest selection', () => {
  it('stays below provider tool limits', () => {
    expect(selectFounderToolNames('help me run the business').length).toBeLessThanOrEqual(MAX_FOUNDER_DIRECT_TOOLS)
    expect(MAX_FOUNDER_DIRECT_TOOLS).toBeLessThan(128)
  })

  it('keeps job-autonomy tools visible for a job application request', () => {
    const names = selectFounderToolNames('Start applying for jobs for me. Up to 150 qualified applications per day.')
    expect(names).toContain('start_job_applications')
    expect(names.some((name) => /job|application/i.test(name))).toBe(true)
  })
})
''')

          Path('lib/caye-direct-activity.ts').write_text(r'''import 'server-only'
import { createServiceClient } from '@/lib/supabase-server'

export type CayeDirectActivityKind = 'thinking' | 'analyzing_image' | 'calling_tool' | 'completed' | 'failed'

export interface CayeDirectActivity {
  id: string
  workspace_id: string
  thread_id: string
  kind: CayeDirectActivityKind
  label: string | null
  tool_name: string | null
  updated_at: string
}

export async function startCayeDirectActivity(input: {
  workspaceId: string
  threadId: string
  kind: CayeDirectActivityKind
  label?: string | null
}): Promise<string | null> {
  try {
    const supabase = createServiceClient()
    const { data, error } = await supabase
      .from('caye_direct_turn_activity')
      .insert({
        workspace_id: input.workspaceId,
        thread_id: input.threadId,
        kind: input.kind,
        label: input.label ?? null,
      })
      .select('id')
      .single()
    if (error) {
      console.warn('[caye-direct-activity] start failed:', error.message)
      return null
    }
    return typeof data?.id === 'string' ? data.id : null
  } catch (err) {
    console.warn('[caye-direct-activity] start threw:', err)
    return null
  }
}

export async function updateCayeDirectActivity(
  id: string | null | undefined,
  patch: { kind: CayeDirectActivityKind; label?: string | null; toolName?: string | null }
): Promise<void> {
  if (!id) return
  try {
    const supabase = createServiceClient()
    const { error } = await supabase
      .from('caye_direct_turn_activity')
      .update({
        kind: patch.kind,
        label: patch.label ?? null,
        tool_name: patch.toolName ?? null,
        updated_at: new Date().toISOString(),
      })
      .eq('id', id)
    if (error) console.warn('[caye-direct-activity] update failed:', error.message)
  } catch (err) {
    console.warn('[caye-direct-activity] update threw:', err)
  }
}

export async function latestCayeDirectActivity(workspaceId: string, threadId?: string | null): Promise<CayeDirectActivity | null> {
  const supabase = createServiceClient()
  let query = supabase
    .from('caye_direct_turn_activity')
    .select('id, workspace_id, thread_id, kind, label, tool_name, updated_at')
    .eq('workspace_id', workspaceId)
    .in('kind', ['thinking', 'analyzing_image', 'calling_tool'])
    .gte('updated_at', new Date(Date.now() - 10 * 60_000).toISOString())
  if (threadId) query = query.eq('thread_id', threadId)
  const { data, error } = await query.order('updated_at', { ascending: false }).limit(1).maybeSingle()
  if (error || !data) return null
  return data as CayeDirectActivity
}
''')

          Path('app/api/founder/caye-direct/activity/route.ts').parent.mkdir(parents=True, exist_ok=True)
          Path('app/api/founder/caye-direct/activity/route.ts').write_text(r'''import { NextRequest, NextResponse } from 'next/server'
import { requireFounder } from '@/lib/founder'
import { latestCayeDirectActivity } from '@/lib/caye-direct-activity'

export async function GET(req: NextRequest) {
  const user = await requireFounder(req)
  if (!user) return NextResponse.json({ error: 'Forbidden' }, { status: 403 })

  const workspaceId = req.nextUrl.searchParams.get('workspaceId')
  const threadId = req.nextUrl.searchParams.get('threadId')
  if (!workspaceId) return NextResponse.json({ error: 'workspaceId is required' }, { status: 400 })

  const activity = await latestCayeDirectActivity(workspaceId, threadId)
  return NextResponse.json({ activity })
}
''')

          Path('components/dashboard/caye-direct/CayeActivityBridge.tsx').write_text(r''' 'use client'

import { useEffect } from 'react'
import { getSession } from '@/lib/supabase'

function humanToolLabel(toolName: string | null): string {
  if (!toolName) return 'Using a tool'
  if (/job|application|resume|candidate|ats/i.test(toolName)) return 'Working on job search'
  if (/research|intelligence|evidence|claim|belief/i.test(toolName)) return 'Researching'
  if (/image|vision|photo/i.test(toolName)) return 'Analyzing image'
  if (/email|gmail|inbox|draft/i.test(toolName)) return 'Checking email'
  const words = toolName.replace(/^get_|^list_|^read_|^inspect_/, '').replace(/_/g, ' ')
  return `Using ${words}`
}

function labelFor(activity: { kind?: string; label?: string | null; tool_name?: string | null } | null): string {
  if (!activity) return 'Thinking'
  if (activity.label) return activity.label
  if (activity.kind === 'analyzing_image') return 'Analyzing image'
  if (activity.kind === 'calling_tool') return humanToolLabel(activity.tool_name ?? null)
  return 'Thinking'
}

/**
 * Keeps the existing inline CayeWorkingIndicator honest without duplicating
 * the chat UI. The server records real turn/tool state; this bridge only
 * mirrors that state into the label already rendered beside Caye's mark.
 */
export default function CayeActivityBridge({ workspaceId }: { workspaceId: string }) {
  useEffect(() => {
    let cancelled = false
    let timer: ReturnType<typeof setTimeout> | null = null

    async function poll() {
      try {
        const label = document.querySelector<HTMLElement>('.caye-working-label')
        if (!label) {
          timer = setTimeout(poll, 500)
          return
        }
        const { session } = await getSession()
        if (!session || cancelled) return
        let threadId: string | null = null
        try { threadId = window.localStorage.getItem(`caye-command-selected-thread:${workspaceId}`) } catch {}
        const qs = new URLSearchParams({ workspaceId })
        if (threadId) qs.set('threadId', threadId)
        const res = await fetch(`/api/founder/caye-direct/activity?${qs.toString()}`, {
          headers: { Authorization: `Bearer ${session.access_token}` },
          cache: 'no-store',
        })
        if (!cancelled && res.ok) {
          const json = await res.json()
          const next = labelFor(json.activity ?? null)
          label.textContent = next
          label.setAttribute('aria-label', `Caye is ${next.toLowerCase()}`)
        }
      } catch {
        // Status is cosmetic. A polling failure must never disturb the turn.
      } finally {
        if (!cancelled) timer = setTimeout(poll, 500)
      }
    }

    poll()
    return () => {
      cancelled = true
      if (timer) clearTimeout(timer)
    }
  }, [workspaceId])

  return null
}
''')

          Path('supabase/migrations/20260831_caye_direct_turn_activity.sql').write_text(r'''-- Ephemeral founder-visible turn activity for Caye Direct.
-- Service-role only: this is operational telemetry, not customer data.
create table if not exists public.caye_direct_turn_activity (
  id uuid primary key default gen_random_uuid(),
  workspace_id uuid not null,
  thread_id uuid not null,
  kind text not null check (kind in ('thinking','analyzing_image','calling_tool','completed','failed')),
  label text,
  tool_name text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index if not exists idx_caye_direct_turn_activity_thread_updated
  on public.caye_direct_turn_activity (workspace_id, thread_id, updated_at desc);

alter table public.caye_direct_turn_activity enable row level security;
-- Intentionally no client RLS policies. Founder API routes use the service role
-- after requireFounder(); customers can never read or write this table directly.
''')

          # Route the Founder Direct manifest through a bounded deterministic selector.
          replace(
              'lib/model-router/caye-direct-bridge.ts',
              "import { runFounderToolLoop } from './tool-bridge/founder-tool-loop'\n",
              "import { runFounderToolLoop } from './tool-bridge/founder-tool-loop'\nimport { selectFounderToolNames } from './tool-bridge/founder-tool-selector'\n",
          )
          replace(
              'lib/model-router/caye-direct-bridge.ts',
              "  responseStyle?: 'voice'\n}",
              "  responseStyle?: 'voice'\n  activityId?: string | null\n}",
          )
          replace(
              'lib/model-router/caye-direct-bridge.ts',
              "  Object.assign(toolCtx, { founderUserId: args.founderUserId })\n",
              "  Object.assign(toolCtx, { founderUserId: args.founderUserId, directActivityId: args.activityId ?? null })\n",
          )
          replace(
              'lib/model-router/caye-direct-bridge.ts',
              "      restrictToToolNames: args.restrictToToolNames,\n",
              "      restrictToToolNames: args.restrictToToolNames ?? selectFounderToolNames(args.message),\n",
          )

          # Thread turn carries the activity id into the router tool context.
          replace(
              'lib/caye-agent/founder-thread-turn.ts',
              "  responseStyle?: 'voice'\n}",
              "  responseStyle?: 'voice'\n  /** Ephemeral founder-visible progress row for this Direct turn. */\n  activityId?: string | null\n}",
          )
          replace(
              'lib/caye-agent/founder-thread-turn.ts',
              "      responseStyle: options!.responseStyle,\n",
              "      responseStyle: options!.responseStyle,\n      activityId: options!.activityId ?? null,\n",
          )

          # Tool execution writes real calling-tool state when a Direct turn supplied activity context.
          replace(
              'lib/caye-agent/orchestrator.ts',
              "import { classifyToolResponse } from './response-intent'\n",
              "import { classifyToolResponse } from './response-intent'\nimport { updateCayeDirectActivity } from '@/lib/caye-direct-activity'\n",
          )
          replace(
              'lib/caye-agent/orchestrator.ts',
              "  const startedAt = Date.now()\n",
              "  const startedAt = Date.now()\n  const directActivityId = (ctx as ToolContext & { directActivityId?: string | null }).directActivityId\n  if (directActivityId) {\n    void updateCayeDirectActivity(directActivityId, { kind: 'calling_tool', toolName: tool.name })\n  }\n",
          )
          replace(
              'lib/caye-agent/orchestrator.ts',
              "  void logToolCall({ tool, args, ctx: scopedCtx, meta, result: last, attempts, durationMs }).catch(\n    (err) => console.error('[orchestrator] tool-call log write failed:', err)\n  )\n\n  return { result: last, attempts, durationMs }\n",
              "  void logToolCall({ tool, args, ctx: scopedCtx, meta, result: last, attempts, durationMs }).catch(\n    (err) => console.error('[orchestrator] tool-call log write failed:', err)\n  )\n  if (directActivityId) {\n    void updateCayeDirectActivity(directActivityId, { kind: 'thinking' })\n  }\n\n  return { result: last, attempts, durationMs }\n",
          )

          # Start/finish activity around typed Direct turns. This is best-effort telemetry only.
          replace(
              'app/api/founder/caye-direct/threads/[id]/route.ts',
              "import { resolveRichResultReferences } from '@/lib/caye-direct-rich-result-resolution'\n",
              "import { resolveRichResultReferences } from '@/lib/caye-direct-rich-result-resolution'\nimport { startCayeDirectActivity, updateCayeDirectActivity } from '@/lib/caye-direct-activity'\n",
          )
          replace(
              'app/api/founder/caye-direct/threads/[id]/route.ts',
              "  try {\n    const authoritativeWorkspaceId = await resolveAuthoritativeThreadWorkspace(supabase, threadId)\n",
              "  let activityId: string | null = null\n  try {\n    const authoritativeWorkspaceId = await resolveAuthoritativeThreadWorkspace(supabase, threadId)\n",
          )
          replace(
              'app/api/founder/caye-direct/threads/[id]/route.ts',
              "    const requestedMode = VALID_REQUESTED_MODES.find((m) => m === model) ?? 'auto'\n    const result = await runFounderThreadTurn(\n",
              "    const requestedMode = VALID_REQUESTED_MODES.find((m) => m === model) ?? 'auto'\n    activityId = await startCayeDirectActivity({\n      workspaceId: turnWorkspaceId,\n      threadId,\n      kind: attachments?.length ? 'analyzing_image' : 'thinking',\n    })\n    const result = await runFounderThreadTurn(\n",
          )
          replace(
              'app/api/founder/caye-direct/threads/[id]/route.ts',
              "      { requestedMode, founderUserId: user.id },\n",
              "      { requestedMode, founderUserId: user.id, activityId },\n",
          )
          replace(
              'app/api/founder/caye-direct/threads/[id]/route.ts',
              "    return NextResponse.json({\n      ...result,\n",
              "    await updateCayeDirectActivity(activityId, { kind: 'completed' })\n    return NextResponse.json({\n      ...result,\n",
          )
          replace(
              'app/api/founder/caye-direct/threads/[id]/route.ts',
              "  } catch (err) {\n    const msg = err instanceof Error ? err.message : 'Agent failed'\n",
              "  } catch (err) {\n    await updateCayeDirectActivity(activityId, { kind: 'failed' })\n    const msg = err instanceof Error ? err.message : 'Agent failed'\n",
          )

          # Mount the activity bridge on the founder dashboard. No change for customer owners.
          replace(
              'app/dashboard/[workspaceId]/page.tsx',
              "import FounderHome from '@/components/dashboard/founder-home/FounderHome'\n",
              "import FounderHome from '@/components/dashboard/founder-home/FounderHome'\nimport CayeActivityBridge from '@/components/dashboard/caye-direct/CayeActivityBridge'\n",
          )
          replace(
              'app/dashboard/[workspaceId]/page.tsx',
              "        {isFounder ? <FounderHome /> : <HomeScreen />}\n",
              "        {isFounder ? <>\n          <CayeActivityBridge workspaceId={workspaceId} />\n          <FounderHome />\n        </> : <HomeScreen />}\n",
          )

          # Remove this one-shot workflow from the final diff.
          Path('.github/workflows/zz-caye-direct-tool-routing-activity.yml').unlink()
          PY
      - name: Install dependencies
        run: npm ci
      - name: Focused tests
        run: npx vitest run lib/model-router/tool-bridge/founder-tool-selector.test.ts
      - name: Build
        run: npm run build
      - name: Commit generated patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix Founder Direct tool routing and live activity status"
          git push origin HEAD:fix/caye-direct-tool-routing-activity
